import React from "react";
import { View, Text } from "react-native";
import Styles from "./styles";

function ChecklistForm(props) {

    return (
        <View style={Styles.componentContainer}>
            <Text>Checklist Form</Text>
        </View>
    );
}

export default ChecklistForm;